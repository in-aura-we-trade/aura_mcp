# TypeScript Example

AI agents should connect to this server through stdio:

```json
{
  "mcpServers": {
    "aura": {
      "command": "aura-mcp",
      "args": ["serve"]
    }
  }
}
```

The server is local. It is not a hosted HTTP wrapper.
